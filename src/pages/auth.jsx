/**
 * Render the authentication page.
 * @returns {JSX.Element} The top-level JSX element that renders a div with the text "Auth Page".
 */
export default function Auth(){
    return <div>Auth Page</div>
}